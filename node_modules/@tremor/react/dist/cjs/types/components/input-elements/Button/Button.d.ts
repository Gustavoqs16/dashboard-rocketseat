import React from "react";
import { ButtonType, Color, HorizontalPosition, Importance, ButtonVariant, MarginTop, Size } from "../../../lib";
export interface ButtonIconOrSpinnerProps {
    loading: boolean;
    iconSize: string;
    iconPosition: string;
    Icon: React.ElementType | undefined;
    transitionState: string;
}
export declare const ButtonIconOrSpinner: ({ loading, iconSize, iconPosition, Icon, transitionState, }: ButtonIconOrSpinnerProps) => JSX.Element;
export interface ButtonProps {
    type?: ButtonType;
    text?: string;
    value?: any;
    icon?: React.ElementType;
    iconPosition?: HorizontalPosition;
    size?: Size;
    color?: Color;
    importance?: Importance;
    variant?: ButtonVariant;
    handleClick?: () => void;
    onClick?: React.MouseEventHandler<HTMLButtonElement>;
    onSubmit?: React.FormEventHandler<HTMLButtonElement>;
    onReset?: React.FormEventHandler<HTMLButtonElement>;
    marginTop?: MarginTop;
    disabled?: boolean;
    loading?: boolean;
    loadingText?: string;
    children?: React.ReactNode;
}
declare const Button: ({ type, text, value, icon, iconPosition, handleClick, onClick, onSubmit, onReset, size, color, importance, variant, marginTop, disabled, loading, loadingText, children, }: ButtonProps) => JSX.Element;
export default Button;
