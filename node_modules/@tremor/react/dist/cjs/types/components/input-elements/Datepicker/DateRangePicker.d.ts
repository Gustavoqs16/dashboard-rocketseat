/// <reference types="react" />
import { enUS } from "date-fns/locale";
import { Color, MarginTop, MaxWidth } from "../../../lib/inputTypes";
export declare type Locale = typeof enUS;
export declare type DateRangePickerValue = [
    (Date | null)?,
    (Date | null)?,
    (string | null)?
];
export declare type DateRangePickerOption = {
    value: string;
    text: string;
    startDate: Date;
    endDate?: Date;
};
export interface DateRangePickerProps {
    value?: DateRangePickerValue;
    defaultValue?: DateRangePickerValue;
    onValueChange?: (value: DateRangePickerValue) => void;
    enableDropdown?: boolean;
    options?: DateRangePickerOption[];
    minDate?: Date | null;
    maxDate?: Date | null;
    placeholder?: string;
    dropdownPlaceholder?: string;
    enableYearPagination?: boolean;
    color?: Color;
    marginTop?: MarginTop;
    maxWidth?: MaxWidth;
    locale?: Locale;
}
declare const DateRangePicker: ({ value, defaultValue, onValueChange, enableDropdown, options, minDate, maxDate, placeholder, dropdownPlaceholder, color, marginTop, maxWidth, enableYearPagination, locale, }: DateRangePickerProps) => JSX.Element;
export default DateRangePicker;
