import React, { Dispatch, SetStateAction } from "react";
import { MarginTop } from "../../../lib";
interface ExpandedContextValue {
    isExpanded: boolean;
    setIsExpanded: Dispatch<SetStateAction<boolean>> | undefined;
}
export declare const ExpandedContext: React.Context<ExpandedContextValue>;
export interface AccordionProps {
    shadow?: boolean;
    expanded?: boolean;
    marginTop?: MarginTop;
    children: React.ReactElement[] | React.ReactElement;
}
declare const Accordion: ({ shadow, expanded, marginTop, children, }: AccordionProps) => JSX.Element;
export default Accordion;
